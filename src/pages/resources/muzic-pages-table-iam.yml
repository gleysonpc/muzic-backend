PagesTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
    - dynamodb:Query
  Resource:
    - ${self:custom.PagesTable.arn}
    - !Join ['/', ['${self:custom.PagesTable.arn}', 'index', 'pageBySlug']]

